FROM node:20-alpine

WORKDIR /app

# pnpmを最新版にアップデート
RUN npm install -g pnpm@latest

# 依存関係ファイルのみをコピー
COPY package.json pnpm-lock.yaml ./

# シンプルなインストールコマンド
RUN pnpm install

# ソースコードをコピー
COPY . .

# 本番用ビルド
RUN pnpm run build

EXPOSE 3000

CMD ["pnpm", "start"] 
